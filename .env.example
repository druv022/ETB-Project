PURDUE_API_KEY=your-purdue-token-here

# Streamlit admin (optional). Docker Compose defaults to admin/admin if unset — override for production.
# ETB_ADMIN_USERNAME=admin
# ETB_ADMIN_PASSWORD=change-me
